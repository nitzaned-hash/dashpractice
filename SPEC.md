# SPEC — דאשבורד פניות שירות לקוחות

## 1. מטרה

דאשבורד מבוסס דפדפן להצגה וניתוח של פניות שירות לקוחות (tickets): מצב נוכחי של העומס, זמני תגובה ופתרון, עמידה ב-SLA, שביעות רצון לקוחות וביצועי צוות השירות — הכל במסך אחד, ניתן לסינון וחיפוש בזמן אמת.

**קהל יעד:** מנהל/ת צוות שירות לקוחות או נציג/ה שרוצים תמונת מצב יומית מהירה, בלי להיכנס למערכת ticketing מלאה.

**גרסה נוכחית:** MVP עם נתוני דמו קבועים (לא מחובר למערכת אמיתית). ראו [§8](#8-הרחבות-עתידיות) להרחבות.

## 2. עקרונות תכן

- **Zero install** — HTML/CSS/JS טהור, ללא framework, ללא build step, ללא תלות ב-Node/Python. פתיחה ישירה של `index.html` עובדת.
- **Zero dependencies חיצוניות** — גם הגרפים בנויים כ-SVG פנימי (`js/charts.js`), כדי שהדאשבורד יעבוד גם ללא אינטרנט.
- **תגובתיות מיידית** — כל שינוי בסינון מרנדר מחדש את כל הרכיבים (KPIs, טבלה, גרפים, ביצועי נציגים) בו-זמנית, ללא צורך ב-refresh.
- **RTL-first** — כל העיצוב וה-layout בנויים עבור עברית מלכתחילה (`dir="rtl"`), לא כתרגום של גרסה אנגלית.

## 3. מודל הנתונים

כל פנייה (ticket) היא אובייקט עם השדות הבאים (מוגדר ב-[js/data.js](js/data.js)):

| שדה | טיפוס | תיאור |
|---|---|---|
| `id` | string | מזהה ייחודי, פורמט `TCK-####` |
| `subject` | string | נושא הפנייה |
| `category` / `categoryLabel` | string | קטגוריה: חיוב, טכני, משלוחים, החזרים, ניהול חשבון, כללי |
| `priority` | string | דחופה / גבוהה / בינונית / נמוכה |
| `slaHours` | number | יעד השעות לפתרון לפי עדיפות (4/8/24/48) |
| `status` | string | פתוחה / בטיפול / ממתינה ללקוח / נפתרה / סגורה |
| `channel` | string | אימייל / צ'אט / טלפון / רשתות חברתיות / טופס אתר |
| `agentId` / `agentName` | string | הנציג/ה המטפל/ת |
| `customerName` / `customerEmail` | string | פרטי הלקוח |
| `createdAt` | Date | מועד פתיחת הפנייה |
| `firstResponseAt` / `firstResponseMinutes` | Date / number | מועד וזמן התגובה הראשונה |
| `resolvedAt` / `resolutionHours` | Date / number | מועד וזמן הפתרון |
| `satisfaction` | 1–5 / null | דירוג CSAT (רק לפניות שנפתרו/נסגרו) |
| `slaBreached` | boolean | האם חרגה מיעד ה-SLA (בין אם עדיין פתוחה ובין אם נפתרה) |

**נתוני דמו:** 220 פניות על פני 45 הימים האחרונים, מיוצרות ב-RNG דטרמיניסטי (seed קבוע) — כך שכל טעינה מציגה את אותו סט נתונים, אך יחסי לתאריך הנוכחי.

## 4. פיצ'רים

### 4.1 כרטיסי KPI
סה"כ פניות, פניות פתוחות, זמן תגובה ממוצע, זמן פתרון ממוצע, שביעות רצון ממוצעת (CSAT), אחוז עמידה ב-SLA. כרטיס ה-SLA מסומן באדום כאשר האחוז נמוך מ-90%.

### 4.2 סינון וחיפוש
- חיפוש חופשי (debounced) לפי מספר פנייה / נושא / שם לקוח / אימייל.
- סינון לפי סטטוס, עדיפות, קטגוריה, נציג.
- טווח תאריכים: 7 / 30 / 45 ימים / הכל.
- כל הסינונים משולבים (AND) ומשפיעים על כל שאר הרכיבים בדף.
- כפתור איפוס חוזר לברירת המחדל (30 ימים, ללא סינון נוסף).

### 4.3 טבלת פניות
עמודות: מס' פנייה, נושא+לקוח, קטגוריה, עדיפות, סטטוס (עם סימון ⚠ לחריגת SLA), נציג, תאריך פתיחה (יחסי), שביעות רצון. מיון לפי כל עמודה (לחיצה חוזרת הופכת כיוון), עימוד של 12 שורות לעמוד.

### 4.4 מודאל פרטי פנייה
לחיצה על שורה פותחת חלון עם: כל שדות הפנייה, תגי סטטוס/עדיפות/קטגוריה/SLA, וציר זמן (נפתחה → תגובה ראשונה → נפתרה).

### 4.5 גרפים
- **מגמת פניות** — גרף קו ל-14 הימים האחרונים: פניות חדשות מול פניות שנפתרו.
- **פילוח קטגוריות** — גרף עמודות אופקי.
- **פילוח סטטוסים** — גרף דונאט עם legend.

### 4.6 ביצועי נציגים
טבלה לכל נציג/ה: סה"כ פניות, פתוחות, נפתרו (עם mini-bar), זמן פתרון ממוצע, שביעות רצון ממוצעת, מספר חריגות SLA.

### 4.7 מצב תצוגה
מתג בין מצב בהיר לכהה, נשמר ב-`localStorage` לפעם הבאה.

## 5. חישובי SLA (הגדרה מדויקת)

| עדיפות | יעד שעות לפתרון |
|---|---|
| דחופה | 4 |
| גבוהה | 8 |
| בינונית | 24 |
| נמוכה | 48 |

- פנייה **שנפתרה**: חריגה = `resolutionHours > slaHours`.
- פנייה **פתוחה/בטיפול/ממתינה ללקוח**: חריגה = הזמן שחלף מאז הפתיחה כבר עולה על `slaHours` (חריגה "בזמן אמת", גם אם טרם נפתרה).
- "עמידה ב-SLA" ב-KPI מחושבת על כלל הפניות הרלוונטיות (פתוחות + שנפתרו) בטווח הנבחר.

## 6. ארכיטקטורה טכנית

```
index.html          מבנה הדף, טוען את 3 קבצי ה-JS בסדר קבוע
css/styles.css       עיצוב, משתני CSS לצבעים (--primary וכו'), תמיכת dark mode
js/data.js           מקור האמת לנתונים — window.DASHBOARD_DATA
js/charts.js         3 פונקציות רינדור גרפים גנריות (window.Charts)
js/app.js            state ניהול סינון + כל לוגיקת הרינדור
serve.ps1            שרת סטטי אופציונלי (PowerShell, ללא תלות ב-Node/Python)
```

**זרימת נתונים:** `data.js` יוצר את `TICKETS` פעם אחת בטעינה → `app.js` מסנן/ממיין לפי `state` → כל פונקציית `render*` מקבלת את הרשימה המסוננת ומצירת מחדש את חלקה בדף. אין framework, אין virtual DOM — רינדור מלא מחדש של כל רכיב בכל שינוי (מספיק ביצועית בסדר גודל של מאות פניות).

**תלות בין קבצים:** `data.js` → `charts.js` → `app.js` (סדר טעינה קריטי — `app.js` מניח ששני הקודמים כבר טענו).

## 7. דרישות לא-פונקציונליות

- **פעולה ללא שרת** — הדף חייב לעבוד גם כשנפתח ישירות מהכונן (`file://`), כל עוד אין קריאות `fetch`.
- **ביצועים** — רינדור מלא מחדש (כולל גרפי SVG) תחת 100ms עבור עד ~500 פניות.
- **נגישות שפה** — כל הממשק בעברית, RTL מלא, כולל כיווניות בגרפים ובטבלאות.
- **תאימות דפדפן** — כל דפדפן מודרני (Chrome/Edge/Firefox); לא נבדק ב-IE.

## 8. הרחבות עתידיות (לא כלול ב-MVP)

- חיבור למקור נתונים אמיתי (CSV/JSON מיוצא, או API כמו Zendesk/Intercom/HubSpot) — ידרוש הרצה משרת (בגלל חסימת CORS על `fetch` מ-`file://`).
- ייצוא נתונים מסוננים ל-CSV.
- התראות/סימון פניות שעומדות לחרוג מ-SLA בקרוב (לא רק חריגה בפועל).
- ניהול משתמשים/הרשאות (כרגע דאשבורד קריאה-בלבד, ללא login).
- שמירת מצב סינון בין טעינות (למשל ב-URL query params).

## 9. מחוץ לתחום (Non-goals)

- אין יצירה/עריכה/מחיקה של פניות מתוך הדאשבורד — תצוגה בלבד.
- אין ניהול משתמשים או הרשאות.
- אין אינטגרציה בזמן אמת (real-time) — הנתונים נטענים פעם אחת בטעינת הדף.
